{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile within the library system, storing non-authentication related information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "email": {
          "type": "string",
          "description": "User's email address, used for communication (not authentication).",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The user's first name."
        },
        "lastName": {
          "type": "string",
          "description": "The user's last name."
        },
        "libraryCardNumber": {
          "type": "string",
          "description": "Unique identifier for the user's library card."
        },
        "membershipStatus": {
          "type": "string",
          "description": "The current status of the user's library membership (e.g., 'active', 'suspended', 'expired')."
        },
        "joinedDate": {
          "type": "string",
          "description": "The date and time when the user joined the library.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "firstName",
        "lastName",
        "libraryCardNumber",
        "membershipStatus",
        "joinedDate"
      ]
    },
    "Author": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Author",
      "type": "object",
      "description": "Represents an author of books.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Author entity."
        },
        "name": {
          "type": "string",
          "description": "The full name of the author."
        },
        "biography": {
          "type": "string",
          "description": "A brief biography or description of the author."
        },
        "dateOfBirth": {
          "type": "string",
          "description": "The author's date of birth.",
          "format": "date"
        },
        "dateOfDeath": {
          "type": "string",
          "description": "The author's date of death, if applicable.",
          "format": "date"
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Genre": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Genre",
      "type": "object",
      "description": "Represents a category or genre for books.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Genre entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the genre (e.g., 'Fantasy', 'Science Fiction', 'History')."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the genre."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Book": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Book",
      "type": "object",
      "description": "Represents a book available in the library.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Book entity."
        },
        "title": {
          "type": "string",
          "description": "The title of the book."
        },
        "isbn": {
          "type": "string",
          "description": "The International Standard Book Number (ISBN), a unique commercial book identifier."
        },
        "publicationYear": {
          "type": "number",
          "description": "The year the book was published."
        },
        "publisher": {
          "type": "string",
          "description": "The name of the publisher."
        },
        "description": {
          "type": "string",
          "description": "A summary or synopsis of the book."
        },
        "availableCopies": {
          "type": "number",
          "description": "The number of copies currently available for loan."
        },
        "totalCopies": {
          "type": "number",
          "description": "The total number of copies of this book owned by the library."
        },
        "authorId": {
          "type": "string",
          "description": "Reference to the Author of the book. (Relationship: Author 1:N Book)"
        },
        "genreIds": {
          "type": "array",
          "description": "References to Genres associated with this book. (Relationship: Genre N:N Book)",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "title",
        "isbn",
        "publicationYear",
        "publisher",
        "description",
        "availableCopies",
        "totalCopies",
        "authorId"
      ]
    },
    "Loan": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Loan",
      "type": "object",
      "description": "Represents an instance of a book being borrowed by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Loan entity."
        },
        "bookId": {
          "type": "string",
          "description": "Reference to the Book that was loaned. (Relationship: Book 1:N Loan)"
        },
        "userProfileId": {
          "type": "string",
          "description": "Reference to the UserProfile who borrowed the book. (Relationship: UserProfile 1:N Loan)"
        },
        "loanDate": {
          "type": "string",
          "description": "The date and time the book was borrowed.",
          "format": "date-time"
        },
        "dueDate": {
          "type": "string",
          "description": "The date and time the book is due to be returned.",
          "format": "date-time"
        },
        "returnDate": {
          "type": "string",
          "description": "The actual date and time the book was returned, if applicable.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the loan (e.g., 'borrowed', 'returned', 'overdue')."
        }
      },
      "required": [
        "id",
        "bookId",
        "userProfileId",
        "loanDate",
        "dueDate",
        "status"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "/user_profiles/{userProfileId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores user-specific profile information. The `userProfileId` directly corresponds to the Firebase Auth UID (`request.auth.uid`), providing denormalized ownership for authorization independence."
    },
    "/authors/{authorId}": {
      "schema": {
        "$ref": "#/entities/Author"
      },
      "description": "Contains public information about authors. Accessible for read by all authenticated users. Write access is restricted to administrative roles."
    },
    "/genres/{genreId}": {
      "schema": {
        "$ref": "#/entities/Genre"
      },
      "description": "Contains public information about book genres. Accessible for read by all authenticated users. Write access is restricted to administrative roles."
    },
    "/books/{bookId}": {
      "schema": {
        "$ref": "#/entities/Book"
      },
      "description": "Contains public information about books available in the library. Accessible for read by all authenticated users. Write access is restricted to administrative roles."
    },
    "/users/{userId}/loans/{loanId}": {
      "schema": {
        "$ref": "#/entities/Loan"
      },
      "description": "Stores individual loan records, nested under the user's path. The `userId` in the path (which must match `request.auth.uid`) provides denormalized ownership for authorization independence. The `userProfileId` field within the document should match the `userId` from the path."
    }
  }
}